# Angular

```bash
# Terminal
npm i -g @angular/cli
ng new [project-name]

...

ng generate component [component-name]
ng add [dependency-name]

...

npm start
npm run build

```

```json
// package.json
{
  "scripts": {
    "start": "ng serve --open",
    "build": "ng build --prod"
  }
}
```
