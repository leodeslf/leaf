# React

```bash
# Terminal
npx create-react-app [project-name]

...

npm start
npm run build
```

```json
// package.json
{
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build"
  }
}
```
